{% extends 'base.html.twig' %}

{% block title %}QuizzDesigner{% endblock %}

{% block header %} {{ parent() }} {% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
    <div class="example-wrapper">
        <h1 class="title text-center">Bienvenue dans Quizz Designer</h1>
        <div class="container quizzList">
            <div class="row">
                {% for quizz in quizzList %}
                    <div class="col-md-4">
                        <div class="card quizzCard">
                            <div class="card-header">
                                <h4 class="card-title text-center">{{ quizz.nom}}</h4>
                            </div>
                            <div class="card-body">
                                {#<p class="card-text">Questions : {{quizz.nbquestion}}</p>#}
                                <p class="card-text">Difficulté : {{quizz.difficulte}}</p>

                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
